---
import BaseLayout from '../layouts/BaseLayout.astro';
import Signup from '../components/Signup.astro';
---

<BaseLayout
  title="The Club — The Little AI Company"
  description="The Little AI Company is focusing on shipping software. Follow the workshop for releases, build notes, and future community plans."
>
  <section class="club-hero">
    <span class="eyebrow">// the little ai club</span>
    <h1>The club can wait. We're building.</h1>
    <p class="lede">
      We still like the idea of a useful community around the work. But we are not going
      to pretend a membership is the product. Right now, the job is to ship Bellamente,
      Agent Relay, and whatever earns its way out of the workshop next.
    </p>
    <a class="btn btn-primary" href="/projects">See what we're building →</a>
  </section>

  <section id="waitlist" class="waitlist">
    <h2>Follow the workshop.</h2>
    <p>
      Get releases, honest build notes, and the occasional practical guide. If a community
      grows around the products later, people following the work will hear about it first.
    </p>
    <Signup source="club-waitlist" subject="TLAC build notes signup" cta="Keep me posted" />
    <small class="hint">No spam. No fake countdown.</small>
  </section>
</BaseLayout>

<style>
  .club-hero { max-width: 720px; padding: 56px 0 26px; }
  .club-hero h1 { font-size: clamp(34px, 5.2vw, 56px); font-weight: 900; margin: 16px 0 0; }
  .lede { color: var(--muted); font-size: 18.5px; margin: 18px 0 24px; max-width: 58ch; }
  .waitlist {
    margin-top: 26px; padding: 28px 24px; border: 1px solid var(--line);
    border-radius: var(--radius); background: var(--paper);
  }
  .waitlist h2 { font-size: 24px; font-weight: 600; }
  .waitlist p { color: var(--muted); margin: 8px 0 16px; max-width: 60ch; }
  .hint { display: block; margin-top: 10px; color: var(--muted); font-size: 13px; }
</style>
